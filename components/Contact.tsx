import { ArrowUpRight, Mail, MessageCircle } from "lucide-react";
import { siteConfig } from "@/lib/config";
import { createEmailContactUrl, createWhatsAppContactUrl } from "@/lib/contactLinks";
import { Reveal } from "./Reveal";
export function Contact(){const wa=createWhatsAppContactUrl(siteConfig.whatsapp),email=createEmailContactUrl(siteConfig.email);return <section id="contact" className="pb-28"><div className="container-page"><Reveal><div className="relative overflow-hidden rounded-[32px] border border-gold/20 bg-gradient-to-br from-[#1d1a12] to-[#0a0b09] px-6 py-16 text-center md:px-16 md:py-24"><div className="absolute -right-20 -top-20 h-80 w-80 rounded-full bg-gold/10 blur-[90px]"/><p className="eyebrow">Personal assistance</p><h2 className="section-title relative mt-4">Need help?</h2><p className="relative mx-auto mt-6 max-w-xl text-sm leading-7 text-white/50">Our team is available to help you choose a plan or configure your device.</p><div className="relative mt-9 flex flex-wrap justify-center gap-3">{wa?<a className="btn-primary" href={wa} target="_blank" rel="noopener noreferrer"><MessageCircle size={17}/> WhatsApp <ArrowUpRight size={15}/></a>:<span className="btn-secondary cursor-not-allowed opacity-50" title="Add a valid number in lib/config.ts"><MessageCircle size={17}/> WhatsApp soon</span>}{email?<a className="btn-secondary" href={email}><Mail size={17}/> Email <ArrowUpRight size={15}/></a>:<span className="btn-secondary cursor-not-allowed opacity-50" title="Add a valid email in lib/config.ts"><Mail size={17}/> Email soon</span>}</div></div></Reveal></div></section>}
